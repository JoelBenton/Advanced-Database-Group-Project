import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "@/styles/globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Health Clinic Appointment Management System (HCAMS)",
  description: "A web-based platform for managing patient appointments, doctor schedules, and medical records using MongoDB. Built to streamline clinic operations and enhance patient care.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
